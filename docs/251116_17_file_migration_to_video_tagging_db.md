# File Migration to Video_tagging_db

**Дата:** 2025-11-16 17:04
**Задача:** Разделение кодовой базы на ANPR и Video Tagging подсистемы

---

## 🎯 Цель миграции

Перенести все файлы, связанные с Video Tagging, на два уровня вверх в директорию `/mnt/e/Projects/Quantum/Video_tagging_db/`, чтобы отделить функциональность тегирования видео от ANPR (штрафы/номера).

---

## 📊 Результаты миграции

### ✅ ПЕРЕНЕСЕНО (22 файла)

Файлы **удалены** из `pipeline/` и **перемещены** в `Video_tagging_db/`:

#### 📹 Video Tagging Core (8 файлов):
- `phase1_whisper_frames.py` - Фаза 1: Whisper транскрипция + извлечение кадров
- `phase2_vllm_analysis.py` - Фаза 2: VLLM анализ видео
- `vllm_client.py` - Клиент для VLLM API (Qwen 2.5 VL)
- `vllm_scene_director.py` - Умная нарезка видео на сцены
- `chunked_video_analyzer.py` - Анализ 50-сек блоков видео
- `run_vllm_analysis.py` - Запуск VLLM анализа
- `scene_analyzer.py` - Анализ сцен через OpenCLIP

#### 🎵 Audio/Video Processing (3 файла):
- `audio_extractor.py` - Извлечение аудио сегментов
- `audio_transcriber.py` - Транскрибация аудио (Whisper)
- `video_transcriber.py` - Транскрибация видео

#### 🌐 Translation & Utilities (5 файлов):
- `translator.py` - Перевод транскриптов через LLM
- `hunyuan_translator.py` - Перевод через Hunyuan-MT-7B
- `performance_analyzer.py` - Анализ производительности Phase 2
- `progress_monitor.py` - Мониторинг прогресса Phase 2
- `debug_vllm.py` - Отладка VLLM сегментов

#### 🔧 Shared Modules (6 файлов):
- `main.py` - Главный entry point для видео тегирования
- `face_processor.py` - Детекция лиц + кластеризация
- `object_detector.py` - YOLO детекция объектов
- `frame_extractor.py` - Извлечение кадров из видео
- `pose_detector.py` - Детекция поз/жестов (MediaPipe)
- `result_exporter.py` - Экспорт результатов в CSV

---

### ✅ СКОПИРОВАНО (10 файлов)

Файлы **остались** в `pipeline/` и **скопированы** в `Video_tagging_db/`:

#### 🚗 ANPR Modules (9 файлов):
- `anpr_detector.py` - YOLO детектор номеров
- `anpr_processor.py` - ANPR процессор: детекция + OCR + валидация
- `anpr_walker.py` - Обработка директорий с видео для ANPR
- `unified_anpr_system.py` - Единая система ANPR для штрафов
- `plate_detector.py` - Детектор номерных знаков
- `plate_recognizer.py` - OCR распознавание номеров
- `vehicle_yolo_detector.py` - Детекция транспорта (YOLO)
- `vehicle_compcars_analyzer.py` - Классификация марок/моделей авто
- `pdf_generator.py` - Генерация PDF отчетов о нарушениях

**Причина копирования:** ANPR модули нужны в обеих системах:
- В `pipeline/` - для штрафов и веб-интерфейса
- В `Video_tagging_db/` - для детекции номеров в видео + будущая детекция марок машин

#### ⚙️ Config (1 файл):
- `config_manager.py` - Управление конфигурацией (.env)

**Причина копирования:** Общий модуль конфигурации для обеих систем

---

### 🧪 ПЕРЕНЕСЕНО В EXPERIMENTS

- `ocr_easy.py` → `pipeline/experiments/ocr_easy.py`

**Причина:** Устаревший/неиспользуемый код

---

## 📁 Текущая структура

### Video_tagging_db/ (31 файл):
```
/mnt/e/Projects/Quantum/Video_tagging_db/
├── phase1_whisper_frames.py
├── phase2_vllm_analysis.py
├── vllm_client.py
├── vllm_scene_director.py
├── chunked_video_analyzer.py
├── run_vllm_analysis.py
├── scene_analyzer.py
├── audio_extractor.py
├── audio_transcriber.py
├── video_transcriber.py
├── translator.py
├── hunyuan_translator.py
├── performance_analyzer.py
├── progress_monitor.py
├── debug_vllm.py
├── main.py
├── face_processor.py
├── object_detector.py
├── frame_extractor.py
├── pose_detector.py
├── result_exporter.py
├── anpr_detector.py (copied)
├── anpr_processor.py (copied)
├── anpr_walker.py (copied)
├── unified_anpr_system.py (copied)
├── plate_detector.py (copied)
├── plate_recognizer.py (copied)
├── vehicle_yolo_detector.py (copied)
├── vehicle_compcars_analyzer.py (copied)
├── pdf_generator.py (copied)
└── config_manager.py (copied)
```

### pipeline/ (11 файлов):
```
/mnt/e/Projects/Quantum/Video_tagging/pipeline/
├── anpr_detector.py (original)
├── anpr_processor.py (original)
├── anpr_walker.py (original)
├── unified_anpr_system.py (original)
├── plate_detector.py (original)
├── plate_recognizer.py (original)
├── vehicle_compcars_analyzer.py (original)
├── vehicle_yolo_detector.py (original)
├── pdf_generator.py (original)
├── config_manager.py (original)
├── ocr_processor.py (не используется)
└── experiments/
    └── ocr_easy.py
```

---

## ⚠️ Что осталось сделать

### 1. Проверить ocr_processor.py
- Файл остался в `pipeline/`
- Не используется ни ANPR, ни video tagging
- **Решение:** Переместить в `experiments/` или удалить

### 2. Обновить импорты
После переноса файлов могут сломаться импорты:
- В `Video_tagging_db/` проверить все `import` и `from ... import`
- Возможно понадобится `sys.path` или относительные импорты

### 3. Тестирование
- Запустить `phase1_whisper_frames.py` из `Video_tagging_db/`
- Запустить `phase2_vllm_analysis.py` из `Video_tagging_db/`
- Проверить, что ANPR в `pipeline/` работает

### 4. Документация
- Обновить README в обеих директориях
- Указать зависимости между проектами

---

## 📝 Команды для проверки

### Проверка Video_tagging_db:
```bash
cd /mnt/e/Projects/Quantum/Video_tagging_db
ls -1 *.py | wc -l  # Должно быть 31
```

### Проверка pipeline:
```bash
cd /mnt/e/Projects/Quantum/Video_tagging/pipeline
ls -1 *.py | wc -l  # Должно быть 11
```

### Поиск битых импортов:
```bash
cd /mnt/e/Projects/Quantum/Video_tagging_db
grep -r "from \.\." *.py  # Проверить относительные импорты
```

---

## ✅ Преимущества новой структуры

1. **Разделение ответственности:**
   - `pipeline/` - только ANPR + штрафы + веб-интерфейс
   - `Video_tagging_db/` - полноценная система тегирования видео

2. **Независимая разработка:**
   - Можно развивать video tagging отдельно
   - ANPR модули доступны в обеих системах

3. **Переиспользование кода:**
   - ANPR модули скопированы для детекции номеров в видео
   - В будущем - детекция марок авто в видео

4. **Упрощение деплоя:**
   - `pipeline/anpr_deploy/` деплоит только ANPR веб-интерфейс
   - Video tagging может иметь свой деплой

---

## 🎯 Следующие шаги

1. [ ] Переместить `ocr_processor.py` в `experiments/`
2. [ ] Проверить и исправить импорты в `Video_tagging_db/`
3. [ ] Запустить тесты в обеих директориях
4. [ ] Создать `README.md` в `Video_tagging_db/`
5. [ ] Обновить документацию проекта

---

**Миграция выполнена:** 2025-11-16 17:04
**Статус:** ✅ Завершена (требуется тестирование)
